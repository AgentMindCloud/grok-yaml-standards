{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://grokyaml.dev/schemas/v1.2.0/grok-workflow.schema.json",
  "title": "grok-workflow v1.2.0",
  "description": "Official JSON Schema for grok-workflow.yaml — defines multi-step automated workflows with conditional branching, error handling, and composable actions. Compatible with grok-install.yaml@1.0+ and grok@2026.4+.",
  "type": "object",
  "required": ["version", "author", "compatibility", "workflows"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "description": "Semantic version of this workflow configuration file.",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "examples": ["1.2.0"]
    },
    "author": {
      "type": "string",
      "description": "X handle of the workflow config owner, prefixed with '@'.",
      "pattern": "^@[A-Za-z0-9_]{1,50}$"
    },
    "compatibility": {
      "type": "array",
      "description": "Platform specs this file is tested and compatible with.",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9._-]+@[0-9]+\\.[0-9]+(\\+|\\.[0-9]+)?$"
      }
    },
    "workflows": {
      "type": "object",
      "description": "Named workflow definitions. Each key is the workflow identifier used in '@grok run workflow:<Name>' triggers.",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "description": "A single multi-step automated workflow.",
        "required": ["description", "steps"],
        "additionalProperties": false,
        "properties": {
          "description": {
            "type": "string",
            "description": "Human-readable explanation of what this workflow accomplishes end-to-end.",
            "minLength": 10,
            "maxLength": 500
          },
          "trigger": {
            "type": "string",
            "description": "Canonical @grok trigger string used to invoke this workflow from any X context.",
            "pattern": "^@grok .+",
            "examples": ["@grok run workflow:ReleasePipeline"]
          },
          "timeout_minutes": {
            "type": "integer",
            "description": "Maximum wall-clock minutes the entire workflow may run before being force-stopped.",
            "minimum": 1,
            "maximum": 1440,
            "default": 30
          },
          "on_failure": {
            "type": "string",
            "description": "Global failure strategy applied when any step fails and the step has no local on_error override.",
            "enum": ["abort", "skip", "retry", "notify_user"],
            "default": "abort"
          },
          "notify_on_complete": {
            "type": "boolean",
            "description": "Post an X notification when the workflow finishes (success or failure).",
            "default": false
          },
          "steps": {
            "type": "array",
            "description": "Ordered sequence of actions executed top-to-bottom. Steps share a runtime context and can reference previous step outputs.",
            "minItems": 1,
            "maxItems": 100,
            "items": {
              "type": "object",
              "description": "A single workflow step.",
              "required": ["name", "action"],
              "additionalProperties": false,
              "properties": {
                "name": {
                  "type": "string",
                  "description": "Short display name for this step shown in workflow progress output.",
                  "minLength": 1,
                  "maxLength": 100,
                  "pattern": "^[a-z][a-z0-9_-]*$"
                },
                "action": {
                  "type": "string",
                  "description": "Action to execute. Can be a grok spec name (e.g. 'grok-test'), a tool identifier, or a prompt library key.",
                  "minLength": 1,
                  "maxLength": 100
                },
                "input": {
                  "type": "string",
                  "description": "Static input string passed to the action (e.g. 'all' for grok-test).",
                  "maxLength": 1000
                },
                "template": {
                  "type": "string",
                  "description": "Prompt library key to use when the action is 'grok-prompts' or 'publish_thread'.",
                  "pattern": "^[a-z_][a-z0-9_]*$"
                },
                "prompt": {
                  "type": "string",
                  "description": "Prompt library key to use when action is 'grok-prompts'.",
                  "pattern": "^[a-z_][a-z0-9_]*$"
                },
                "condition": {
                  "type": "string",
                  "description": "JavaScript-style boolean expression evaluated against previous step outputs. Step is skipped when false.",
                  "maxLength": 512,
                  "examples": ["steps.RunTests.exit_code === 0", "steps.SecurityScan.alerts.length === 0"]
                },
                "on_error": {
                  "type": "string",
                  "description": "Error handling strategy for this specific step. Overrides the workflow-level on_failure setting.",
                  "enum": ["abort", "skip", "retry", "notify_user"]
                },
                "retry": {
                  "type": "object",
                  "description": "Retry configuration. Only used when on_error is 'retry'.",
                  "additionalProperties": false,
                  "properties": {
                    "max_attempts": {
                      "type": "integer",
                      "description": "Maximum number of retry attempts before the step is considered failed.",
                      "minimum": 1,
                      "maximum": 10
                    },
                    "backoff": {
                      "type": "string",
                      "description": "Delay growth strategy between retry attempts.",
                      "enum": ["none", "linear", "exponential"],
                      "default": "exponential"
                    },
                    "delay_seconds": {
                      "type": "integer",
                      "description": "Initial delay in seconds before the first retry (or fixed delay when backoff is 'none').",
                      "minimum": 1,
                      "maximum": 3600
                    }
                  }
                },
                "timeout_minutes": {
                  "type": "integer",
                  "description": "Per-step timeout in minutes. Overrides the workflow-level timeout for this step.",
                  "minimum": 1,
                  "maximum": 120
                },
                "timeout_seconds": {
                  "type": "integer",
                  "description": "Per-step timeout in seconds. Takes precedence over timeout_minutes when both are set.",
                  "minimum": 1,
                  "maximum": 86400
                },
                "approval_required": {
                  "type": "boolean",
                  "description": "Pause the workflow and require human approval before this step executes. Always use for steps that post to X, deploy to production, or open PRs.",
                  "default": false
                },
                "depends_on": {
                  "type": "array",
                  "description": "Step names that must complete successfully before this step starts.",
                  "uniqueItems": true,
                  "maxItems": 20,
                  "items": {
                    "type": "string",
                    "description": "Name of a step defined in the same workflow."
                  }
                },
                "env": {
                  "type": "object",
                  "description": "Environment variables made available to this step's action.",
                  "additionalProperties": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
